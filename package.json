{
  "name": "my-mini-bakthi-book",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "node-fetch": "^3.3.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "netlify dev",
    "prebuild": "rimraf build",
    "build": "react-scripts build",
    "postbuild": "npm run build:functions",
    "build:functions": "cd netlify/functions && npm install --production && cd -",
    "test": "react-scripts test",
    "test:function": "node test-function-direct.js",
    "test:all": "npm test && npm run test:function",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0",
    "rimraf": "^3.0.2"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
